{
  "name": "mega-login",
  "version": "0.0.2",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/index.js --platform=node",
    "build:dev": "esbuild src/index.ts --bundle --outfile=dist/index.js --platform=node",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  },
  "dependencies": {
    "@cemalgnlts/mailjs": "^3.0.1"
  }
}
